#include <stdio.h>  // Include standard input and output library

int main() {        // Main function where the execution of program begins
	char message[] = "Hello, World!";  // Declare a character array and initialize it with the message
	printf("%s\n", message);  // Print the message stored in the variable
	return 0;       // Return 0 to the operating system
}
